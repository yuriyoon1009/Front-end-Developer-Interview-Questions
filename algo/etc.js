// insertion sort
// selection sort
// shell sort
// change sort
// heap 정렬
// array vs linkedlist
console.log(`array와 linkedlist 공통점은 순차적으로
데이터를 저장 할 수 있다.`);
console.log(`array는 데이터를 저장할 공간을 미리 저장해야한다.`);
console.log(`linkedlist 동적으로 줄였다 늘였다 할 수 있다.`);
// stack and queue
// tree
// binary tree
console.log(`binary tree`, `한개의 노드에 최대 2개 이하의
자식을 소유하고 있는 트리`);
// full binary tree
console.log(`full binary tree`, `모든 노드가 2개의 자식 노드를
가지며 모든 레벨이 꽉 찬 트리이다.`);
// complete binary tree
console.log(`complete binary tree`, `포화 이진 트리같이
모든 레벨이 꽉 찬 트리는 아니지만 모든 노드가 2개의 자식 노드를 가지는
트리이다.`);
console.log(`http://minsone.github.io/programming/binary-tree`);
// bst (binary search tree)
// binary heep
// graph